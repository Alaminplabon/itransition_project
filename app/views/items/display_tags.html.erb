<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Items</title>
</head>
<body class="bg-gray-100 text-gray-800">
<div class="container mx-auto my-8 p-4">
  <div class="text-center mb-8">
    <h1 class="text-3xl font-bold text-gray-800">Items</h1>
  </div>

  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    <% @items.each do |item| %>
      <div class="bg-white rounded-lg shadow-lg overflow-hidden">
        <div class="p-6">
          <h2 class="text-xl font-bold mb-2"><%= item.name %></h2>


          <div class="flex justify-between">
            <%= link_to 'Show', collection_item_path(item.collection,item), class: 'bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-lg transition duration-300 ease-in-out transform hover:scale-105' %>

          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="text-center mt-8">
    <%#= link_to 'New Collection', new_collection_item_path(), class: 'inline-block bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-lg shadow-lg transition duration-300 ease-in-out transform hover:scale-105' %>
  </div>
</div>
</body>
</html>
