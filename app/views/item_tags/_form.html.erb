<%= form_with(model: item_tag, local: true) do |form| %>
  <% if item_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_tag.errors.count, "error") %> prohibited this item tag from being saved:</h2>

      <ul>
        <% item_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.collection_select :item_id, Item.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.collection_select :tag_id, Tag.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
